```sql
select distinct ``,``,``
from ``
where xx=xx
order by `` ASC / DESC
limit 2

```



```
count(name)计数
COUNT(DISTINCT column_name)
IFNULL(region_name,0)
as xxx作为新的字段
```

```
INNER JOIN
LEFT JOIN
```

```
AVG(st_score)求得平均数

round(AVG(st_score),2)，对平均数四舍五入保留两位小数
```

